{
  "name": "instagram-account-checker-yasmin",
  "version": "2.2.0",
  "private": true,
  "description": "Sequential Instagram account existence checker by Yasmin Almeida, with no PIN screen and Coin Elite tools preserved.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check api/health.js && node --check api/check-instagram.js"
  },
  "engines": {
    "node": ">=20"
  }
}
